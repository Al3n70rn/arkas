# Sample .travis.yml for R projects.
#
# See README.md for instructions, or for more configuration options,
# see the wiki:
#   https://github.com/craigcitro/r-travis/wiki

language: c

pandoc: true

sudo: required

dist: trusty

env:
    matrix:
    - BIOC_USE_DEVEL="FALSE"


before_install:
  - curl -OL http://raw.github.com/craigcitro/r-travis/master/scripts/travis-tool.sh
  - chmod 755 ./travis-tool.sh
  - ./travis-tool.sh bootstrap
  - sudo apt-get -y update -qq
  - sudo apt-get --yes install gcc-4.6-base cpp-4.6 libgomp1 libquadmath0 libc6-dev build-essential zlib1g-dev libc6-dev libcurl4-openssl-dev libxml2-dev
  - sudo apt-get --yes install curl cmake zlibc zlib1g-dev libhdf5-dev
  - sudo apt-get install -y git
  - sudo apt-get install -y pandoc
install:
    - ./travis-tool.sh install_bioc devtools ensembldb rtracklayer Biobase Biostrings GenomicRanges OrganismDbi Rsamtools 
    - sudo R -e 'library(devtools);install_github("arcolombo/TxDbLite")'
    - ./travis-tool.sh install_bioc_deps
    - ./travis-tool.sh install_bioc rhdf5 matrixStats GenomicRanges GenomicFeatures Matrix KEGGREST
    - ./travis-tool.sh install_bioc rmarkdown roxygen2 knitr
script:
    #run the vignette
     - sudo R -e 'library(rmarkdown);render(system.file("extdata", "artemis.Rmd", package="artemis"))' 
    # - R -e 'library(rmarkdown); render("geneWiseAnalysis.Rmd")'
    # - R -e 'library(rmarkdown); render("pcaPlot.Rmd")'
after_failure:
  - ./travis-tool.sh dump_logs

notifications:
  email:
    on_success: change
    on_failure: change
