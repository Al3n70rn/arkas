## if using something like RepBase, structure repeat classes hierarchically
annotateRepeats <- function(res, repbase) {
  
  if (!grepl("RepBase", repbase)) stop("You must specify a RepBase assembly!")

  stop("Not done yet...")

}
